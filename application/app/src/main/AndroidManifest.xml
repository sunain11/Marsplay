<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sunain.marsplay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:configChanges="screenSize|orientation"
        android:icon="@drawable/marsplayicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/marsplayicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/SplashTheme" >
        </activity>
        <activity
            android:name=".Activity.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.AuthActivity"
            android:theme="@style/SplashTheme" />
        <activity android:name=".Activity.UploadProductActivity" />
        <activity android:name=".Activity.ProductListActivity" />
        <activity
            android:name=".Activity.EditImageActivity"
            android:theme="@style/SplashTheme" />
        <activity android:name=".Activity.ProductActivity"></activity>
    </application>

</manifest>